; Overrides used only by scripts/serve-local.ps1.
; Keeping PHP startup warnings out of HTTP responses is required by Livewire,
; because upload endpoints must return valid JSON.
upload_tmp_dir="${LOCAL_PHP_UPLOAD_TMP}"
display_errors=Off
display_startup_errors=Off
log_errors=On

